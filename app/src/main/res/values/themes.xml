<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for Light mode -->
    <style name="Theme.Gymtimer" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/primaryGreen</item>
        <item name="colorPrimaryVariant">@color/primaryPurple</item>
        <item name="colorOnPrimary">@color/text</item>

        <!-- Secondary colors -->
        <item name="colorSecondary">@color/secondaryLightGreen</item>
        <item name="colorSecondaryVariant">@color/secondaryLightPurple</item>
        <item name="colorOnSecondary">@color/text</item>

        <!-- Background and text colors -->
        <item name="android:windowBackground">@color/background</item>
        <item name="colorSurface">@color/background</item>
        <item name="colorOnBackground">@color/text</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/background</item>
    </style>

    <style name="Theme.Gymtimer.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Gymtimer.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Gymtimer.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
